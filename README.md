# Product Catalog Service

Консольное приложение для управления каталогом товаров с поддержкой регистрации, аутентификации, CRUD операций, кэширования и аудита действий пользователей.

## 📋 Описание

Внутренний сервис для управления каталогом товаров. Система позволяет:
- Регистрировать и авторизовывать пользователей
- Добавлять, изменять, удалять и просматривать товары
- Искать товары по различным параметрам
- Кэшировать часто запрашиваемые данные для ускорения доступа
- Вести аудит всех действий пользователей

## 🚀 Технологии

- **Java 17** (Java Core + Collections)
- **Gradle** — система сборки
- **Lombok** — генерация boilerplate кода

## ⚙️ Установка и запуск

### Требования
- JDK 17 или выше
- Gradle (или использовать встроенный wrapper)

### Клонирование репозитория
```shell
git clone https://github.com/LockDown488/YlabTask1.git
cd YlabTask1
```

### Сборка проекта
```shell
./gradlew build
```

### Запуск приложения
```shell
./gradlew run
```

## 🎯 Функциональность

### Авторизация
- Регистрация нового пользователя
- Вход в систему
- Выход из системы

### Управление каталогом товаров
- **Добавление товара** — создание нового товара с полями: название, категория, бренд, цена, описание
- **Изменение товара** — обновление существующего товара по ID
- **Удаление товара** — удаление товара по ID
- **Просмотр каталога** — вывод всех товаров
- **Поиск по фильтрам** - фильтрация товаров по категории, бренду или ценовому диапазону
- **Поиск по ID** — получение конкретного товара с использованием кэша

### Кэширование
- LRU-кэш на основе `LinkedHashMap` для ускорения повторных запросов
- Автоматическая очистка кэша при изменении каталога
- Ограничение размера кэша (50 элементов)

### Аудит
- Логирование всех действий пользователей (вход, выход, CRUD операции)
- Сохранение времени, пользователя, типа действия и деталей
- Просмотр журнала аудита